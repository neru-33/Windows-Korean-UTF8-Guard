interface:
  display_name: "Windows UTF-8 Guard"
  short_description: "Opt-in guard for Windows console mojibake"
  default_prompt: "Use $windows-utf8-guard only when Windows PowerShell/cmd encoding issues appear."
